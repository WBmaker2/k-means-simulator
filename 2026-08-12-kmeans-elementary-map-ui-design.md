# K-means 초등 학습 화면 디자인 설계

## 목표

초등학생이 화면을 열었을 때 무엇을 해야 하는지 5초 안에 이해하고, `K값 정하기 → 한 단계 실행 → 모둠 변화 관찰 → K=2~6 비교` 흐름을 큰 시각 단위로 따라갈 수 있는 학습 화면으로 개선한다. 메인 진입 파일은 `index.html`로 통일한다.

## 현재 문제

- 공개 화면에서 로컬 CSS가 적용되지 않으면 기본 브라우저 스타일로 노출되어 학습 사이트의 구조와 색상 체계가 사라진다.
- 기존 화면은 작은 보조 텍스트와 많은 카드가 동시에 보여 핵심 조작인 `한 단계씩 실행`이 약하게 보인다.
- 단계 진행, 지도 캔버스, K값 비교가 서로 다른 정보처럼 보여 학습 순서가 한눈에 연결되지 않는다.
- K값을 숫자 입력과 슬라이더로만 조작해 초등학생이 현재 선택값을 빠르게 인지하기 어렵다.

## 선택한 방향: 우리 동네 탐험 지도형

밝고 안정적인 지도 학습 화면을 만든다. 장식보다 학습 행동을 우선하며, 카드와 색상은 `미션`, `조작`, `관찰`, `비교`의 네 역할로 제한한다.

### 시각 언어

- 배경: 아주 옅은 하늘색 `#F4F8FC`
- 기본 글자: 짙은 남색 `#17324D`
- 주요 행동: 파란색 `#2563EB`
- 미션 강조: 노랑 `#F7C948`
- 성공/완료: 민트 `#22B8A7`
- 보조 강조: 산호 `#F97360`
- 카드: 흰색, 18px 내외 모서리, 얕은 그림자
- 글꼴: 시스템 한글 글꼴을 사용하고 제목은 700~800, 본문은 400~600으로 구분한다.
- 외부 CDN, 외부 이미지, 외부 아이콘 의존성은 사용하지 않는다.

### 화면 구조

1. **탐험 헤더**
   - 작은 지도 핀 배지와 제목
   - 한 줄 설명은 데스크톱에서만 보조적으로 노출
   - 테마 선택은 큰 pill 버튼으로 유지하되 선택 상태를 색과 아이콘으로 명확히 표시

2. **미션 히어로**
   - `오늘의 미션` 라벨, 큰 질문형 제목, 짧은 설명
   - 현재 테마 설명을 별도 긴 문단이 아닌 작은 지도 메모 카드로 표시

3. **3단계 학습 진행 바**
   - `1 중심 후보 놓기`, `2 가까운 중심에 묶기`, `3 평균 위치로 옮기기`
   - 현재 단계는 채움 색과 번호 배지로 강조하고 완료 단계는 체크 표시로 전환
   - 단계 아래에 `지금 할 일 / 관찰할 것 / 다음 단계`를 한 줄 카드로 배치

4. **학습 작업 영역**
   - 데스크톱: 왼쪽 조작 패널, 오른쪽 큰 지도 캔버스
   - 모바일: 조작 패널 → 지도 캔버스 → 결과 순서
   - 지도 캔버스 상단에는 `우리 동네 지도에서 찾아보기`와 반복 횟수/거리 요약을 함께 표시
   - 캔버스는 어두운 추상 지도 대신 밝은 종이 지도 톤과 격자, 구역 라벨, 색상 점을 사용

5. **K값 선택 카드**
   - `K값은 시설을 몇 곳 둘지 정하는 숫자예요`를 큰 설명으로 표시
   - `K=2`부터 `K=6`까지 선택 버튼을 한 줄에 제공
   - 슬라이더/숫자 입력은 보조 조작으로 유지하되 선택 버튼보다 시각적 우선순위를 낮춘다.

6. **행동 버튼**
   - 기본: `한 단계 보기`
   - 보조: `자동으로 끝까지`, `처음부터`
   - 버튼 문구는 결과보다 행동을 설명하고, 실행 중에는 disabled 상태와 안내 문구를 제공한다.

7. **결과와 비교**
   - 중심 후보 카드는 색상 배지, 좌표, 참고 수요를 큰 숫자와 함께 보여준다.
   - K=2~6 비교는 표보다 카드/막대형 요약을 우선해 평균 거리와 관리 부담을 비교한다.
   - 마지막에는 `내가 고른 K`와 이유 입력을 학습 활동으로 연결한다.

8. **엔트리 가이드**
   - 본 실습 흐름을 방해하지 않도록 작업 영역 아래의 접이식 보조 학습 카드로 배치한다.
   - 데이터/모델/블록 탭과 CSV 내려받기, 활동지 링크 기능은 유지한다.

## 기능 보존 규칙

- `changeTheme`, `updateK`, `stepSimulation`, `startSimulation`, `resetSimulation`, `runKComparison`, `selectKChoice`, `toggleGuideSection` 등 기존 전역 함수와 이벤트 동작을 유지한다.
- SVG의 `entry-svg`, `line-group`, `data-points-group`, `centroids-group`, `tooltip` ID를 유지한다.
- K 범위는 2~6 비교를 유지하고 현재 구현의 최대값 6을 보존한다.
- CSV 다운로드와 인쇄용 활동지 링크를 유지한다.
- `K-평균-수업-활동지.html` 링크는 그대로 사용한다.

## 파일 구성

- `index.html`: 메인 시뮬레이터. 기존 HTML/JS의 동작을 유지하면서 파일명과 클래스/레이아웃을 정리한다.
- `assets/kmeans-simulator.css`: 외부 프레임워크 없이 위 디자인 토큰과 반응형 레이아웃을 제공한다.
- `assets/kmeans-icons.js`: 로컬 SVG 아이콘 렌더링을 유지한다.
- `design-qa.md`: 같은 viewport의 렌더링 캡처, 검사 상태, 발견점과 수정 이력을 기록한다.

## 검증 기준

- 기본 브라우저 스타일이 아닌 의도한 색상, 카드, 버튼, 레이아웃이 로컬 서버와 Pages에서 보인다.
- 데스크톱에서 미션, 단계 진행, 조작, 지도, 비교 순서가 한 화면의 시각 흐름으로 연결된다.
- 320px 이상 모바일 폭에서 가로 스크롤 없이 주요 조작이 가능하다.
- K=2~6 선택과 한 단계 실행, 자동 실행, 초기화, 테마 전환, 엔트리 가이드 토글이 동작한다.
- 콘솔 오류가 없고 Python 학습 흐름 테스트가 통과한다.
